# 成语接龙 · 古典文人风设计规范（Agent 实现提示词）

> 使用方法：将本文档与参考产物 `idiom-crossword-prototype.html` 一同提供给实现 agent。实现时以本文档为视觉契约，参考 HTML 为信息结构与交互基准，目标平台为 iOS 竖屏手游。

---

## 一句话设计定位

用「宣纸 + 墨 + 朱砂」的古典文人质感，把成语交叉填字的推理乐趣与科举仕途成长体系融为一体；不做现代扁平游戏的糖果色，而是「一本摊开的古籍、一枚朱红落印」的东方文人气度。

## 2. 核心品牌气质（写作时反复对照）

- 克制、有文化厚度、不媚俗。视觉权重给「信息层级」而非「装饰密度」。
- 签名式视觉：**朱砂印章**贯穿「通关 / 完成 / 成就 / 每日挑战」所有达成态，全案只保留这一个决定性视觉母题。
- 书法味来自**字体（宋体衬线）+ 竖排印章 + 笔画留白**，不靠花哨插画或过度渐变。
- 贯穿「科举仕途」内核：士人、书卷、印章、月白文献气质。

## 3. 色彩（六 Token，OKLch 派生）

| Token | 值 | 用途 |
|---|---|---|
| `--bg` 宣纸 | `#F3EDE1` | 页面底色（暖米白，非默认灰底） |
| `--surface` | `#FBF7EE` | 卡片面（略提亮） |
| `--surface-2` | `#EFE7D4` | 交叉格 / 灰底（比普通格深 10%） |
| `--fg` 墨 | `#2B2922` | 正文 |
| `--muted` | `#857C68` | 次级文字 / 灰褐 |
| `--accent` 朱砂红 | `#B33B27` | **唯一强调色**：主按钮 / 印章 / 高亮 / 焦点环 |
| `--accent-deep` | `#8F2C1B` | 下压阴影、深红文字 |
| `--accent-soft / pale` | `#E8CFC0 / #F4E3D8` | 红色系浅底（选中格、红印章衬底） |
| `--gold` 暗金 | `#B08A28` | 每日挑战 / 科举点缀（只用于此，克制） |
| `--leaf` 松青绿 | `#4E6E45` | 正确反馈 |
| `--border` | `#E2D8BE` | 分隔线 / 卡边界 |

**硬性纪律**
- 每屏可见的 `--accent` 不超过 2 处。
- 无紫→蓝信任渐变、无默认 Indigo（`#6366f1`）。红/金仅作功能与科举语义。
- 悬停 / 焦点：背景 L 通道移动 ±0.06–0.12，正文色不可变浅；禁用态是唯一允许降对比的状态。
- 衬线大字红色多用 `--accent-deep` 而非 `--accent`（保证对比）。

## 4. 字体与排版（CJK 强约束）

| 角色 | 字体 | 备注 |
|---|---|---|
| 展示 / 成语 / 标题 | `Noto Serif SC` / `Songti SC` | 书名、成语、数字指标全部用衬线 |
| 正文 / 界面 | `Noto Sans SC` / `PingFang SC` | 收纳说明 |
| 数值大标签 | 衬线加粗 | 大数字用衬线（信息层级关键） |

- **中文（CJK）大标题行高必须 1.3–1.4**，禁止用拉丁 `1.0–1.2`，否则相邻行笔画相接。
- CJK 用 `letter-spacing:0`（禁止负字距压缩汉字）。
- 大写拼音 / 标签文字追踪（letter-spacing）`0.06–0.1em`。
- 正文行高 1.7，行宽限 `max-width:65ch`。
- 全案至多使用宋体工具 + 无衬线体两种字族。

## 5. 布局系统（竖向）

- 竖屏画板 412×900，底部五 Tab：**首页 / 关卡 / 收藏 / 商城 / 我的**。
- 子页面（成就 / 统计 / 设置 / 每日挑战 / 游戏）带顶部「返回」箭头，从对应 Tab 进入。
- 每个视图为独立滚动块，底部内容在 Tab 栏之上（`padding-bottom` 预留 `--tab-h`）。
- 卡片统一圆角 `18px`、细边框 `--border`、轻投影。
- 表达：大标题（深处衬线）+ kicker（英文？——中文；小号青灰 tracking 大写感）+ 内容卡。
- 「印章」组件：朱砂实底 / 空心 / 灰底三种态，竖向书写模式用于 1–2 字。

## 6. 核心组件速查

- **印章 seal**：`writing-mode:vertical-rl`，红底白字圆角，达成态 / 每日 / 当前关卡。
- **成果进度条 xp-track**：`--surface-2` 底 + 朱砂渐增满；文字「经验 数 / 离某衔还差 N」。
- **卡片 tile / 菜单行**：左侧图标盒子 + 标题 + 摘要；右侧 `›` 或轻提示。
- **每日挑战卡**：每日成语大字（衬线900）+ 难度徽章（暗金）+「开始挑战 / 昨日回顾」两只按钮（一主一次）。
- **游戏网格 cell**：单字格子，起始字 `--surface-2` + 右上朱砂角点，空格点到候选；焦点朱红描边 + 外光晕；正确赤绿 / 错误赤红闪烁。
- **过关窗**：中央朱砂「通」大红印章（下沉式红块）+ 经验值与本关成语解释。
- **关卡格子**：完成格带偏移的红色「通」角标；当前格红底白字；未解锁淡灰。

## 7. 统一视觉「母题」与「禁止」

| 做（签名动作） | 改为 |
|---|---|
| 朱砂印章用于所有完成/达成态 | 主按钮、成就角标、过关窗口 |
| 大写数字/成语用衬线 | 数字、成语、关口 |
| 宣纸暖底 + 墨 + 朱红 | 全案默认 |
| 徽章软色（红/金/绿） | 难度 / 状态 |

**停止 fallback 的模仿：**
- 不用紫色渐变、不用 emoji 当图标；图标一律用 1.6–1.8px 的 monoline SVG。
- 不用「红色竖条 + 圆角卡」的 AI dashboard 块。
- 不虚拟指标；无真实数据时用明示占位（如「通关 X / N」）。
- 不写死 raw hex（除本表）；所有衍生色用 `oklch()` / `color-mix()`。
- 每个可指向的区域 / 标题 / 主 CTA / 重复卡片加 `data-od-id="kebab-case-id"`。

## 8. 色彩对照（完整 token 表可直接复制入 `:root`）

已在前文 token 表给出。实现时需补充：正确 `--leaf`、警告、禁用使用 `--surface-2` + `--faint`，禁用仅允许降对比。

## 7. 写作 / 文案基调

- 用词古雅而不晦涩：「科举仕途」「书卷小径」「文房四宝」「今日一读」「水滴石穿」。
- 主 CTA 一句话，次级一律「次要 / 文本链接」，主从不重复逐字。
- 占位符类型（toast、评分）用中文自然语气。

## 9. 实现验收清单（工艺纬度）

- [ ] 中文大标题行高 ≥1.3；CJK 无负字距。
- [ ] 全案只有一个强调色 `--accent`，每屏 ≤2 次可见。
- [ ] 底部五 Tab 17 屏可完整导航；未使用 `scrollIntoView`；无横向滚动。
- [ ] 所有聚焦元素有 `:focus-visible` 红框。
- [ ] 六 Token 用 `oklch()` 派生，>1 原始 hex 放在 `:root` 外。
- [ ] 桌面预览居中手机框；移动端窄屏不横向滚动。
- [ ] 交互态：hover 移背景而非文本变灰；切换态红金绿成对定义。
- [ ] 过关 / 完成统一用「印章」母题，贯穿 App 达成感。

## 11. 字典（美学关键词，供生成 / 命名参考）

`#F3EDE1 宣纸` · `#B33B27 朱砂` · `#2B2922 墨` · `#B08A28 鎏金` · `#4E6E45 松青绿` · 印章 · 书卷 · 科举 · 落款 · 竖排 · 天青色
